# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.2.1
python-multipart==0.0.20

# --- Google AI (mandatory per spec) ---
# Pins loosened so pip resolves a mutually-compatible adk/genai pair. Our code
# uses genai's Client + types.Part.from_bytes + response_mime_type (>=1.0 API).
google-adk>=1.0.0
google-genai>=1.0.0

# --- Config / models ---
pydantic==2.10.5
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Services ---
httpx==0.28.1          # Slack webhook + async HTTP
numpy==2.2.1           # vector similarity for incident memory

# --- Demo asset generation (realistic Grafana snapshot for vision agent) ---
matplotlib==3.10.0

# --- Cloud storage (Phase 3; used when BACKEND=cloud) ---
google-cloud-firestore>=2.16.0
